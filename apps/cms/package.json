{
  "name": "@nexora/cms",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@nexora/schemas": "workspace:*",
    "lucide-react": "1.45.0"
  },
  "scripts": {
    "build": "next build",
    "dev": "next dev --port 48111",
    "lint": "eslint . --max-warnings=0",
    "start": "next start --port 48111",
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
